import {Router}  from "express";const router = new Router();import {categoriesController} from '../controllers/categories.controller.js';import {checkRoleMiddleware} from "../middleware/checkRoleMiddleware.js";import {ROLES} from "../consts/index.js";router.post("/", [checkRoleMiddleware(ROLES.ADMIN)], categoriesController.create);router.put("/:id",[checkRoleMiddleware(ROLES.ADMIN)], categoriesController.update);router.get("/", categoriesController.get);router.get("/:id", categoriesController.getOne);router.delete("/:id",[checkRoleMiddleware(ROLES.ADMIN)], categoriesController.delete);export default router;