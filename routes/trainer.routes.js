import {Router}  from "express"const router = new Router();import {trainerController}  from '../controllers/trainer.controller.js'import {validationMiddleware} from "../middleware/validationMiddleware.js";import {idValidation} from "../validators/validators.js";router.put("/new", [],trainerController.new);router.put("/repeat", [],trainerController.repeat);router.put("/compo", [], trainerController.compo);router.put("/question/:id", [idValidation, validationMiddleware], trainerController.question);router.delete("/category/:id", [idValidation, validationMiddleware], trainerController.resetCategoryProgress);export default router;