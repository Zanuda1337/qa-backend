import {Router}  from "express"const router = new Router();import {usersController}  from '../controllers/users.controller.js'import {authMiddleware}  from '../middleware/authMiddleware.js'import {validationMiddleware} from "../middleware/validationMiddleware.js";import {emailValidation, passwordValidation, oldPasswordValidation} from "../validators/validators.js";router.post("/registration", [emailValidation,passwordValidation, validationMiddleware],usersController.registration);router.post("/login", [emailValidation,passwordValidation, validationMiddleware], usersController.login);router.get("/me", authMiddleware, usersController.me);router.put("/me", [authMiddleware, passwordValidation, validationMiddleware], usersController.deleteAccount);router.put("/me/password", [authMiddleware, passwordValidation, oldPasswordValidation, validationMiddleware], usersController.changePassword);export default router;